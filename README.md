# fast-api-project
Fast API project by rephael4321

In order to start the api run the command 'python main.py'
In order to run the test execute 'pytest test.py'
All environments are depended on a postgres db. Make sure you have one up and running.
